apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: openvpn-web-ui
  labels:
    name: openvpn-web-ui
spec:
  rules:
    - host: vpn.qtosw.com
      http:
        paths:
          - path: /
            backend: 
              servicePort: 8080
              resource:
                kind: Service
                name: openvpn-web-ui

