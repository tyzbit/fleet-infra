apiVersion: v1
kind: Service
metadata:
  name: iperf3-server
spec:
  type: NodePort
  selector:
    app: iperf3-server
  ports:
    - name: http
      protocol: TCP
      port: 5201
      targetPort: 5201
      nodePort: 35201
