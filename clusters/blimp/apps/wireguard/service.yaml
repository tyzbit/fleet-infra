apiVersion: v1
kind: Service
metadata:
  name: wireguard
spec:
  type: NodePort
  selector:
    app: wireguard
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP
  - name: wireguard-udp
    port: 31194
    targetPort: 31194
    nodePort: 31194
    protocol: UDP