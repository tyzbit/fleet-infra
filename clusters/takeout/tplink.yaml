apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tplink
  namespace: flux-system
spec:
  chart:
    spec:
      chart: tplink
      sourceRef:
        kind: HelmRepository
        name: rancher-cd-apps
        namespace: flux-system
  interval: 1m0s
  values:
    namespace: default
    devices:
      - 192.168.1.140
      - 192.168.1.141