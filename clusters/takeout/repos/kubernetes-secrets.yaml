apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: kubernetes-secrets
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  secretRef:
    name: flux-system
  interval: 1m0s
  ref:
    branch: main
  url: https://github.com/tyzbit/kubernetes-secrets
