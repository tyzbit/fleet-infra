apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: doorman-test
  namespace: default
spec:
  accessModes: 
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: "doorman-test"
  resources:
    requests:
      storage: 10Gi
