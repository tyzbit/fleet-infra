apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: keda
  namespace: flux-system
spec:
  targetNamespace: kube-system
  releaseName: keda
  chart:
    spec:
      chart: keda
      version: "2.6.2"
      sourceRef:
        kind: HelmRepository
        name: keda
        namespace: flux-system
  interval: 2m0s
  values: {}
