apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: newrelic-bundle
  namespace: flux-system
spec:
  targetNamespace: monitoring
  releaseName: nri-bundle
  chart:
    spec:
      chart: nri-bundle
      sourceRef:
        kind: HelmRepository
        name: newrelic
        namespace: flux-system
  interval: 1m0s
  values:
    global:
      cluster: takeout
      licenseKey: 904ebcc921b150ac3de8f20d424991f88425NRAL
    infrastructure:
      enabled: true
    kubeEvents:
      enabled: true
    prometheus:
      enabled: true
    nri-prometheus:
      config:
        transformations:
          - description: "Exclude all but kasa metrics"
            ignore_metrics:
              - except:
                - kasa_
    logging:
      enabled: true
    webhook:
      enabled: false