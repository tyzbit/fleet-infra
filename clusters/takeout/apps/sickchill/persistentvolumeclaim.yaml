apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sickchill-config
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: sickchill-config
  accessModes:
    - ReadWriteMany
