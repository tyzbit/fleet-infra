apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-data-nas
  namespace: personal
spec:
  accessModes: 
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: nas
  resources:
    requests:
      storage: 1Gi
