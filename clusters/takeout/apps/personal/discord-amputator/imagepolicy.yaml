apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: discord-amputator
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: discord-amputator
  policy:
    semver:
      range: ">= 1.0.0"
