apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-dl-data
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nfs-fast
  accessModes:
    - ReadWriteMany
