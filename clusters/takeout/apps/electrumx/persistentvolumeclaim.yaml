apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: electrumx-data
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nfs-fast-volatile
  accessModes:
    - ReadWriteMany
