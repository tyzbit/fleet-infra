apiVersion: v1
kind: Service
metadata:
  name: deluge-listener
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: default-deluge
    app.kubernetes.io/name: deluge
  ports:
  - port: 45342
    targetPort: 45342
    protocol: udp
