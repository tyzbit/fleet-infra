apiVersion: v1
kind: Service
metadata:
  name: deluge-listener
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: default-deluge
    app.kubernetes.io/name: deluge
  ports:
  - port: 6881
    targetPort: 6881
    protocol: udp
