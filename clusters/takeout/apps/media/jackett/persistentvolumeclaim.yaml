apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-config-nas
  namespace: default
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas
  accessModes:
    - ReadWriteMany