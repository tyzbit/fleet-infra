apiVersion: v1
kind: Service
metadata:
  name: youtube-dl-synology
  namespace: media
spec:
  type: NodePort
  selector:
    app: youtube-dl-synology
  ports:
    - name: http-youtube-dl
      protocol: TCP
      port: 80
      targetPort: 17442
