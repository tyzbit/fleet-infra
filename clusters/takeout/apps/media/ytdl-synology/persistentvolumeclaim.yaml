apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-dl-data-iscsi
  namespace: media
spec:
  resources:
    requests:
      storage: 40Gi
  volumeMode: Filesystem
  storageClassName: synology-iscsi-storage
  accessModes:
    - ReadWriteOnce
