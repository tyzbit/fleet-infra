apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitcoin-blockchain
  namespace: default
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: temp-nfs-fast-volatile
  accessModes:
    - ReadWriteMany
