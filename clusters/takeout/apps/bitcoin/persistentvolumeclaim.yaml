apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitcoin-blockchain-nas-volatile
  namespace: default
spec:
  resources:
    requests:
      storage: 600Gi
  volumeMode: Filesystem
  storageClassName: nas-volatile
  accessModes:
    - ReadWriteOnce
