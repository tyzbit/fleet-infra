apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dri-pvc
  namespace: games
spec:
  volumeName: dri-games
  accessModes: 
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: ""
  resources:
    requests:
      storage: 256Mi
