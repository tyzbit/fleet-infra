apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: motioneye-config
spec:
  accessModes: 
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: nfs-fast
  resources:
    requests:
      storage: 1Gi
