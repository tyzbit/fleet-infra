apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: motioneye-config-nas
  namespace: media
spec:
  accessModes: 
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: nas
  resources:
    requests:
      storage: 1Gi
