apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temp-filebrowser-config # CHANGEME
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: longhorn # CHANGEME
  accessModes:
    - ReadWriteMany
