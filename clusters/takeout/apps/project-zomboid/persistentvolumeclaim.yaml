apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-zomboid-data
  namespace: default
spec:
  accessModes: 
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
