apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-testing-config
  namespace: media
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
