apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: electrumx-data-nas-volatile
  namespace: default
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas-volatile
  accessModes:
    - ReadWriteMany
