apiVersion: v1
kind: ConfigMap
metadata:
  name: project-zomboid-files-config
  namespace: games
data:
  nginx.conf: |
    server {
      listen 8080 default;
      server_name files.projectzomboid.qtosw.com;
      location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        autoindex on;
      }
    }
