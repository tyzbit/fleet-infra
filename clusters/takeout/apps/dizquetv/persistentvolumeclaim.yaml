apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dizquetv-data
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nfs-fast
  accessModes:
    - ReadWriteMany
