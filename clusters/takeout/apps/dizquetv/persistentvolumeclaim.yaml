apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temp-dizquetv-data
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: temp-nfs-client
  accessModes:
    - ReadWriteMany
