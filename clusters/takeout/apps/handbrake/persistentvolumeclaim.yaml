apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: handbrake-config
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nfs-fast
  accessModes:
    - ReadWriteMany
