apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-metrics-adapter
  namespace: flux-system
spec:
  targetNamespace: kube-system
  chart:
    spec:
      chart: ./charts/kube-metrics-adapter
      version: "0.1.11"
      sourceRef:
        kind: HelmRepository
        name: tyzbit
        namespace: flux-system
  interval: 2m0s
  values:
    enableCustomMetricsApi: true
