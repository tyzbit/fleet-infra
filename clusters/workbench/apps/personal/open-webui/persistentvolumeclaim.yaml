apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui-nas
  namespace: personal
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: nas
  resources:
    requests:
      storage: 10Gi
