apiVersion: v1
kind: Service
metadata:
  name: corpus-control-plane
  namespace: networking
spec:
  type: NodePort
  selector:
    app: corpus-control-plane
  ports:
    - name: k8s
      port: 6443
      protocol: TCP
      targetPort: 6443
      nodePort: 32443
