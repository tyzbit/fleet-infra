---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: volsync
  namespace: flux-system
spec:
  timeout: 15m
  interval: 2m0s
  chart:
    spec:
      chart: volsync
      sourceRef:
        kind: HelmRepository
        name: backube-charts
        namespace: flux-system
      version: "0.13.0"
  values:
    manageCRDs: true
