[hosts]
app = "https://revolt.${public_services_domain}"
api = "https://revolt.${public_services_domain}/api"
events = "wss://revolt.${public_services_domain}/ws"
autumn = "https://revolt.${public_services_domain}/autumn"
january = "https://revolt.${public_services_domain}/january"

[pushd.vapid]
private_key = "${PUSHD_VAPID_PRIVATE_KEY}"
public_key = "${PUSHD_VAPID_PUBLIC_KEY}"

[files]
encryption_key = "${FILES_ENCRYPTION_KEY}"
