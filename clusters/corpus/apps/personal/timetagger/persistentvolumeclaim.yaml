apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timetagger
  namespace: personal
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas
  accessModes:
    - ReadWriteMany
