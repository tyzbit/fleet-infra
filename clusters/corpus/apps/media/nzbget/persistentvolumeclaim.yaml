apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbget-config-longhorn-webapp
  namespace: media
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  storageClassName: longhorn-webapp
  accessModes:
    - ReadWriteOnce
