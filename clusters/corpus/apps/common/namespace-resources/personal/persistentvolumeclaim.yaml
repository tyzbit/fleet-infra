apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-volume1-media
  namespace: personal
spec:
  volumeName: nas-volume1-media-personal
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: ""
  resources:
    requests:
      storage: 256Mi
