apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflared
data:
  config.yaml: |
    tunnel: ${cloudflared_tunnel_uuid}
    credentials-file: /etc/cloudflared/cred.json
    config_src: local
    originRequest:
      connectTimeout: 30s
      noTLSVerify: true
    warp-routing:
      enabled: false

    ingress:
      - service: http://kube-system-ingress-nginx-controller.kube-system
