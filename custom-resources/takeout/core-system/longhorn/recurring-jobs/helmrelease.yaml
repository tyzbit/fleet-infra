apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: longhorn-recurring-jobs
  namespace: flux-system
spec:
  targetNamespace: longhorn-system
  releaseName: longhorn-recurring-jobs
  timeout: 1m
  chart:
    spec:
      chart: longhorn-recurring-jobs
      version: "2.0.1"
      sourceRef:
        kind: HelmRepository
        name: tyzbit
        namespace: flux-system
  interval: 1m0s
  values:
    minuteStep: 5
    hourStep: 2
    createStorageClasses: true
    allowVolumeExpansion: true
    # commented out because this was broken when my StorageClasses were created
    # reclaimPolicy: Retain
    reclaimPolicy: ""
    parameters:
      replicaAutoBalance: "ignored"
      dataLocality: "best-effort"
      numberOfReplicas: "3"
      staleReplicaTimeout: "30"
    groups:
      "Blog":
        snapshot:
          - interval: 10m
            retain: 6
            # concurrency: 10
        backup: 
          - interval: 2h
            retain: 4
      "Crypto":
        parameters:
          # reclaimPolicy: Delete
          numberOfReplicas: "2"
        snapshot:
          - interval: 10m
            retain: 1
      "Database":
        snapshot:
          - interval: 10m
            retain: 6
        backup:
          - interval: 1h
            retain: 6
      "Gameserver":
        parameters:
          numberOfReplicas: "2"
        snapshot:
          - interval: 10m
            retain: 6
          - interval: 1h
            retain: 6
        backup:
          - interval: 2h
            retain: 4
      "Plex":
        snapshot:
          - interval: 10m
            retain: 6
        backup:
          - interval: 2h
            retain: 4
          - interval: 12h
            retain: 4
      "Multimedia":
        backup:
          - interval: 2h
            retain: 4
      "Webapp":
        snapshot:
          - interval: 10m
            retain: 6
        backup:
          - interval: 2h
            retain: 4
          - interval: 12h
            retain: 4