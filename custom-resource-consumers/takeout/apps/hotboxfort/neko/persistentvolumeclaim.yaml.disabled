apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hotboxfort-neko-firefox-profile-nas
  namespace: hotboxfort
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas
  accessModes:
    - ReadWriteOnce
