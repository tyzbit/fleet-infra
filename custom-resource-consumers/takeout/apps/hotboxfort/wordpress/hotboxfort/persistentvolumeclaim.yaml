apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-hotboxfort-nas
  namespace: hotboxfort
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas
  accessModes:
    - ReadWriteMany
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-hotboxfort-mariadb-longhorn-database
  namespace: hotboxfort
spec:
  resources:
    requests:
      storage: 8Gi
  volumeMode: Filesystem
  storageClassName: longhorn-database
  accessModes:
    - ReadWriteOnce
