apiVersion: v1
kind: Service
metadata:
  name: n8n
  namespace: home-automation
spec:
  type: NodePort
  selector:
    app: n8n
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5678
    - name: sqlite
      protocol: TCP
      port: 3000
      targetPort: 3000
