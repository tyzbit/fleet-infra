apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wordpress-vivid
  namespace: flux-system
spec:
  releaseName: wordpress-vivid
  targetNamespace: personal
  timeout: 10m
  chart:
    spec:
      chart: wordpress
      version: "10.10.3"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  interval: 2m0s
  valuesFrom:
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: wordpressUsername
    targetPath: wordpressUsername
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: wordpressPassword
    targetPath: wordpressPassword
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: wordpressEmail
    targetPath: wordpressEmail
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: wordpressTablePrefix
    targetPath: wordpressTablePrefix
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: mariadbAuthRootPassword
    targetPath: mariadb.auth.rootPassword
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: mariadbAuthUsername
    targetPath: mariadb.auth.username
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: mariadbAuthPassword
    targetPath: mariadb.auth.password
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: mariadbAuthDatabase
    targetPath: mariadb.auth.database
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: smtpHost
    targetPath: smtpHost
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: smtpPort
    targetPath: smtpPort
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: smtpUser
    targetPath: smtpUser
  - kind: Secret
    name: wordpress-vivid-values
    valuesKey: smtpPassword
    targetPath: smtpPassword
  values:
    replicaCount: 1 # active longhorn-database nas
    image:
      tag: "5.9.3" # {"$imagepolicy": "flux-system:wordpress-vivid:tag"}
      pullPolicy: Always
      pullSecrets:
      - name: docker-hub
    wordpressBlogName: WP Vivid test
    wordpressConfigureCache: true
    wordpressScheme: https
    allowEmptyPassword: false
    # # Custom .htaccess directives
    # customHTAccessCM: wordpress-vivid-htaccess
    # # Disable .htaccess in apache
    # allowOverrideNone: false
    htaccessPersistenceEnabled: true
    # the rest of the smtp settings are secrets
    smtpProtocol: tls
    livenessProbe:
      enabled: false
    resources:
      requests:
        cpu: 300m
        memory: 512Mi
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: nginx
        cert-manager.io/cluster-issuer: letsencrypt
        acme.cert-manager.io/http01-edit-in-place: "false"
        nginx.ingress.kubernetes.io/proxy-body-size: 256m
        external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
        external-dns.alpha.kubernetes.io/target: ${blog_domain_ddns}
      hostname: "vivid.${blog_domain}"
      tls: true
    service:
      type: NodePort
      externalTrafficPolicy: Local
    persistence:
      existingClaim: wordpress-vivid-nas
    # run a container to fix volume permissions at the start
    volumePermissions:
      enabled: true
    memcached:
      enabled: true
    mariadb:
      primary:
        persistence:
          existingClaim: wordpress-vivid-mariadb-longhorn-database
    affinity:
      podAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - podAffinityTerm:
            labelSelector:
              matchExpressions:
              - key: app.kubernetes.io/instance
                operator: In
                values:
                - wordpress-vivid
            topologyKey: kubernetes.io/hostname
          weight: 100
