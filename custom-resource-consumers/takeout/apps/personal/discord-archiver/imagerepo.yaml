apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImageRepository
metadata:
  name: discord-archiver
  namespace: flux-system
spec:
  secretRef:
    name: docker-hub
  image: docker.io/tyzbit/discord-archiver
  interval: 2m0s
