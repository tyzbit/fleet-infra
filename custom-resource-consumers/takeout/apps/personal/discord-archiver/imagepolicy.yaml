apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: discord-archiver
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: discord-archiver
  policy:
    semver:
      range: ">= 1.0.0"
