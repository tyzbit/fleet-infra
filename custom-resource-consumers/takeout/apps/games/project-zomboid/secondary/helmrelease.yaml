apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: project-zomboid-fb-2-oauth2-proxy
  namespace: games
spec:
  targetNamespace: games
  releaseName: project-zomboid-fb-2-oauth2-proxy
  chart:
    spec:
      chart: oauth2-proxy
      version: "6.12.1"
      sourceRef:
        kind: HelmRepository
        name: oauth2-proxy
        namespace: flux-system
  interval: 2m0s
  values:
    replicaCount: 1
    extraArgs:
      provider: github
      github-org: "${blog_name}"
      github-team: "super-admins,project-zomboid-secondary"
      cookie-expire: 168h0m0s
      scope: "user:email"
    config:
      existingSecret: oauth2-proxy-pz-fb-2
