apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-dl-data-nas
  namespace: media
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: nas
  accessModes:
    - ReadWriteMany
