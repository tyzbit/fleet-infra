apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temp-nfs-storage
spec:
  resources:
    requests:
      storage: 500Gi
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
